# RAG Project - Skeleton

Skeleton project for a Retrieval-Augmented Generation (RAG) system.

Quick start:
- Place text documents under `data/raw/`
- Set environment variables from `.env.example`
- Install dependencies and run the API

See `.env.example` for required config.
